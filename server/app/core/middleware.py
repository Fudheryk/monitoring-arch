from __future__ import annotations
"""server/app/core/middleware.py
~~~~~~~~~~~~~~~~~~~~~~~~
Middlewares globaux (placeholder).
"""
from fastapi import FastAPI

from fastapi import FastAPI

def install_global_middleware(app: FastAPI) -> None:
    pass
